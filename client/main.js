import { Template } from 'meteor/templating';

import '/imports/startup/client';

import '../imports/startup/accounts-config.js';

//tutorial said to place this in but need to see if it's already around
import '../imports/ui/body.js'; //is this actually needed?
